# 接口地址
VITE_GLOB_API_URL=https://localhost/api/admin
VITE_GLOB_WS_URL=wss://localhost/ws

# 是否开启压缩，可以设置为 none, brotli, gzip
VITE_COMPRESS=gzip

# 是否开启 PWA
VITE_PWA=false

# 打包后是否生成dist.zip
VITE_ARCHIVER=true
